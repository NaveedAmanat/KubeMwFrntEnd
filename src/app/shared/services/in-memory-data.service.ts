/*import { InMemoryDbService } from 'angular-in-memory-web-api';

export class InMemoryDataService implements InMemoryDbService {
  createDb() {
    const rules = [
      { id: 1, name: 'rule from in-memory' , category: 'Products', condition: 'Client gender is = M and NDI > Requested Amount / 12',
        status: 'Active', comments: 'Here are some comments'}
    ];
    return {rules};
  }
}*/
